server:
  port: 8189
  servlet:
    context-path: /demo
jwt:
  access-lifetime: 10m
  refresh-lifetime: 30d
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/auth_inv_server
    username: postgres
    password: qwerty12345
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        hbm2ddl:
          auto: update
  main:
    allow-circular-references: true

